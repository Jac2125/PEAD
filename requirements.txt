# PEAD 연구 런타임 의존성
# 버전은 전부 고정. yfinance는 스크래핑 기반이라 마이너 버전에서
# 컬럼 구조가 바뀌므로 범위 지정(>=)은 재현을 보장하지 않음.
# 2026-08-10 기준 PyPI 최신 안정판으로 상호 설치 검증 완료.

# --- 수치 · 자료구조 ---
numpy==2.5.2
pandas==3.0.5
pyarrow==25.0.0          # parquet 스냅샷 입출력

# --- 통계 · 추론 ---
scipy==1.18.0            # ttest_1samp, wilcoxon
statsmodels==0.14.6      # market model OLS, 클러스터 표준오차(cov_type="cluster")

# --- 데이터 수집 ---
yfinance==1.5.2          # 주의: 이전 프로젝트의 0.2.x와 메이저 버전이 다름
requests==2.34.2
requests-cache==1.3.3    # 재실행 시 중복 요청 방지
lxml==6.1.1              # pandas.read_html 백엔드
html5lib==1.1            # 위키피디아 표 파싱 폴백

# --- 설정 · 유틸 ---
PyYAML==6.0.3            # config.yaml 로딩
python-dotenv==1.2.2     # API 키 등 비밀값 분리
tqdm==4.70.0             # 장시간 수집 루프 진행률

# --- 시각화 ---
matplotlib==3.11.1
seaborn==0.13.2